package org.jtwig.spring.csrf.model;

import org.jtwig.model.position.Position;
import org.jtwig.model.tree.Node;

public class CSRFNode extends Node {
    public CSRFNode(Position position) {
        super(position);
    }
}
